require 'spec_helper'

describe Angular::Float::Box do
  it 'has a version number' do
    expect(Angular::Float::Box::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
